# Agent 开发指南

本文件只记录长期有效的工程约束。功能细节以代码为准；面向用户的部署和使用入口见 `README.md`。

## 工作约定

- 使用中文沟通。开始前运行 `git status --short`，保留用户已有修改，不覆盖或回滚无关文件。除非用户明确要求，不要 commit 或 push。
- 使用 TypeScript 和现有 React 结构，保持逻辑清晰，不压行。遵循仓库 Prettier 配置（2 空格、双引号、分号、80 字符目标行宽）；SQL 字符串也应人工合理分行。
- 当前没有正式测试框架。可以使用系统临时目录进行验证，但不要提交临时测试、截图、账号或测试依赖。若以后已有测试体系，只添加必要且简洁的测试。
- 小型修复、样式微调和内部优化不要在 README 中记录“本次改了什么”。只有重要功能变化，或 README 中已有的部署、使用方式和产品事实发生变化时，才更新 README；README 不是 changelog。
- 不在 README 中写用户要求、开发过程或进度汇报。文档修改无需重建应用；代码修改按影响范围完成构建和运行验证。
- 项目按未发布版本维护，不主动设计复杂的旧开发版兼容流程。但不得清空现有账号、漫画、数据库或处理目录，任何重置都需要用户明确授权。

## 技术与结构

- Node.js 22、npm、ES modules；React 19、React Router 7、TypeScript strict、Vite 6；Express 5、SQLite（better-sqlite3）和 Sharp。
- `npm run build` 执行 TypeScript 检查并构建前端到 `dist/`。后端由 `tsx server/index.ts` 运行，Express 同时提供 API、受保护媒体和 SPA。
- 配置使用进程环境变量和 Compose，不引入 `.env`、额外数据库服务、ORM、前端全局状态库或 CSS 框架，除非任务确有需要并得到确认。
- 新 API 必须注册在 SPA fallback 之前。前后端共享的数据形状变化时，同步检查 `server/`、`src/types.ts`、API 调用方和相关页面。

主要代码区域：

- `src/pages/`：书架、详情和设置页面。
- `src/components/`：表单、弹层和通用交互组件。
- `src/reader/`：阅读状态、分页、手势和阅读器样式。
- `server/index.ts`：服务入口与主要 API。
- `server/auth.ts`、`server/users.ts`、`server/access.ts`：认证、用户与权限。
- `server/importer.ts`、`server/images.ts`：扫描、导入和图片处理。
- `server/db.ts`：SQLite schema 与查询辅助。

## 产品边界

- 产品名称是 **NAS 漫画阅读**。修改名称时同步网页标题、PWA manifest 和 iOS 元数据；不要额外增加 logo、宣传语或品牌区块。
- 应用面向自托管漫画阅读，不扩展为社交、小说、阅读历史或离线漫画平台，除非用户明确提出。
- 书架与设置使用亮暗主题；阅读区域始终为黑色。阅读器保留竖屏单页、横屏从右向左双页和纵向滚动三种核心体验。
- 交互需兼顾手机、横屏、安全区、键盘和无障碍；动画遵守 `prefers-reduced-motion`。不要在阅读画面常驻不必要的操作说明。

## 数据与权限

- 原始漫画目录只读。扫描、删除、移除导入源和图片升级都不得修改或删除源文件；平台删除只清理自身记录和派生文件。
- `/manga`、`/data` 和 `/processed` 必须互不重叠。不要把 `/processed` 暴露为无鉴权静态目录，新增派生图片或上传封面时必须关联漫画所有权。
- 管理员拥有管理能力，但不绕过漫画可见性。列表、详情、编辑、封面和 `/media/*` 都必须在后端校验权限；前端隐藏按钮不能代替鉴权。
- 普通用户不能管理用户、导入源或漫画。系统始终保留至少一名管理员。管理员创建用户或重置密码后，用户必须先修改初始密码。
- 会话、密码和写请求继续使用现有安全措施：HttpOnly/SameSite Cookie、token 散列、会话撤销、登录限流和同源校验。不得记录密码、Cookie、API Key、请求体或图片内容。
- 漫画可关联多个导入源，对任一来源授权即可访问。移除来源时必须正确处理共享漫画和保留漫画的授权，不能因批量操作绕过逐项可见性检查。
- 受保护 API 和媒体保持 `private, no-store`；Service Worker 不缓存 `/api/` 或 `/media/`。

## 导入与图片处理

- 导入源限制在素材根目录内，扫描不跟随符号链接。支持手动、一层和二层目录结构；漫画、章节和图片保持字典序。
- “仅刷新新增”只发现新漫画和新章节；“刷新全部”检查已有章节变化和缺失输出。导入不主动删除旧漫画或章节，目录改名视为新内容。
- 手动编辑的标题、作者、日期、标签、封面和章节标题不能被刷新覆盖。标题字段保持日语为主、中文 `title_zh` 独立存储。
- 当前架构只支持单进程、单导入任务。并发必须有界；任务失败时等待已经开始的工作退出后再释放锁，章节处理完成后再原子发布页面列表。
- 图片先校正方向，再按现有规则拆页；保持每页比例，不裁切、不补黑边。原图选项是处理后的无损 PNG，优化图为 WebP。改变页面结构或处理规则时同步考虑缓存签名、缩略图、封面引用和已有章节升级。
- 不随意删除旧处理文件，它们可能仍被当前阅读页面或手动封面引用。

## 阅读器与 PWA

- 横屏双页的前页在右、后页在左，章节之间不能拼成双页；边界翻页可进入相邻章节。
- 拖动应实时跟手并支持完成或回弹。缩放后单指用于移动图片，不触发翻页；手势修改需正确处理 Pointer Capture、取消和多指切换。
- 菜单关闭后不能让隐藏控件取得焦点。相邻页面可预加载，滚动阅读按需加载，禁止一次加载整部漫画原图。
- PWA 的受保护内容不离线缓存。浏览器模拟不能替代 iOS 真机安装、手势和安全区验证，不要把未实际验证的项目声明为通过。

## 开发与验证

常用命令：

```sh
npm ci
npm run check
npm run build
npm run format:check
npm run format
docker compose config --quiet
docker compose up -d --build
docker compose ps
docker compose logs --tail=50
```

- 通常运行 `npm run build` 和 `npm run format:check`。只改 Markdown 时可仅对相关文件运行 Prettier，不要批量格式化整个仓库。
- UI 改动按影响范围检查手机、桌面、横屏、亮暗模式、溢出、焦点和减少动态效果。
- 权限改动至少覆盖普通用户拒绝、管理员不豁免可见性、媒体访问和会话撤销。
- 导入或图片改动使用独立的数据、素材和处理目录验证，不要与正在运行的实例共享数据库。验证后停止自行启动的进程。
- 涉及 Compose 服务时，构建后检查容器状态、健康接口和日志；仅构建镜像不会更新运行中的容器。

可在构建后启动隔离实例：

```sh
scratch_dir="$(mktemp -d)"
mkdir -p "$scratch_dir/manga"
PORT=3002 BIND_ADDRESS=127.0.0.1 \
  MANGA_DIR="$scratch_dir/manga" \
  DATA_DIR="$scratch_dir/data" \
  PROCESSED_DIR="$scratch_dir/processed" \
  npm start
```

首次访问隔离实例时可以创建虚拟管理员；禁止在用户实际实例中创建测试账号、重置密码或执行破坏性验证。

## Docker 与备份

- 容器路径固定为 `/manga`（只读）、`/data`（SQLite）和 `/processed`（派生图片）。主机挂载以 `compose.yml` 为准，不把本机绝对路径写入业务逻辑。
- HTTPS 部署使用 `COOKIE_SECURE: "true"`，HTTP 本机访问使用 `"false"`。
- 重建容器不会删除挂载数据。备份 SQLite 时使用一致性备份，或先停止服务后同时备份数据库与 WAL；完整备份还应包括处理目录和单独保存的原始漫画。
